<odoo>
    <!--
        Enhanced Navigation with Sticky Header
        This improves the navigation experience with a fixed header on scroll
    -->
    
    <!-- Custom Header with Smooth Scroll -->
    <template id="custom_header_scroll" inherit_id="website.layout" name="Tailwind Sticky Header">
        <xpath expr="//header" position="attributes">
            <attribute name="class" add="sticky top-0 z-50 transition-all duration-300 shadow-md" separator=" "/>
            <attribute name="id">main-header</attribute>
        </xpath>
    </template>
    
    <!-- Add Smooth Scroll JavaScript -->
    <template id="smooth_scroll_js" inherit_id="website.layout" name="Smooth Scroll Script">
        <xpath expr="//head" position="inside">
            <style>
                /* Smooth scroll behavior */
                html {
                    scroll-behavior: smooth;
                }
                
                /* Header animation on scroll */
                #main-header.scrolled {
                    background-color: rgba(255, 255, 255, 0.95);
                    backdrop-filter: blur(10px);
                }
                
                #main-header.scrolled.dark {
                    background-color: rgba(17, 24, 39, 0.95);
                }
                
                /* Mobile menu improvements */
                @media (max-width: 768px) {
                    .navbar-collapse {
                        background-color: white;
                        border-radius: 0.5rem;
                        padding: 1rem;
                        margin-top: 0.5rem;
                        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
                    }
                    
                    .dark .navbar-collapse {
                        background-color: rgb(31, 41, 55);
                    }
                }
                
                /* Navigation link hover effects */
                .nav-link {
                    position: relative;
                    transition: color 0.3s ease;
                }
                
                .nav-link:hover {
                    color: #2563eb !important;
                }
                
                .nav-link::after {
                    content: '';
                    position: absolute;
                    bottom: -2px;
                    left: 0;
                    width: 0;
                    height: 2px;
                    background-color: #2563eb;
                    transition: width 0.3s ease;
                }
                
                .nav-link:hover::after {
                    width: 100%;
                }
                
                /* Active link styling */
                .nav-link.active {
                    color: #2563eb !important;
                    font-weight: 600;
                }
                
                /* CTA button in header */
                .header-cta-btn {
                    background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
                    color: white;
                    padding: 0.5rem 1.5rem;
                    border-radius: 0.5rem;
                    font-weight: 600;
                    transition: all 0.3s ease;
                    box-shadow: 0 4px 6px -1px rgba(37, 99, 235, 0.3);
                }
                
                .header-cta-btn:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 10px 15px -3px rgba(37, 99, 235, 0.4);
                    color: white;
                }
            </style>
            <script type="text/javascript">
                // Header scroll effect
                document.addEventListener('DOMContentLoaded', function() {
                    const header = document.getElementById('main-header');
                    if (header) {
                        window.addEventListener('scroll', function() {
                            if (window.scrollY > 50) {
                                header.classList.add('scrolled');
                            } else {
                                header.classList.remove('scrolled');
                            }
                        });
                    }
                    
                    // Active link detection based on scroll position
                    const sections = document.querySelectorAll('section[id]');
                    const navLinks = document.querySelectorAll('.nav-link[href^="#"]');
                    
                    function highlightNav() {
                        let scrollY = window.pageYOffset;
                        
                        sections.forEach(section => {
                            const sectionHeight = section.offsetHeight;
                            const sectionTop = section.offsetTop - 100;
                            const sectionId = section.getAttribute('id');
                            
                            if (scrollY > sectionTop &amp;&amp; scrollY <= sectionTop + sectionHeight) {
                                navLinks.forEach(link => {
                                    link.classList.remove('active');
                                    if (link.getAttribute('href') === '#' + sectionId) {
                                        link.classList.add('active');
                                    }
                                });
                            }
                        });
                    }
                    
                    window.addEventListener('scroll', highlightNav);
                });
            </script>
        </xpath>
    </template>
    
    <!-- Enhanced Navigation Menu -->
    <template id="custom_nav_menu" inherit_id="website.layout" name="Tailwind Navigation Menu">
        <xpath expr="//header//nav[@id='top_menu']" position="replace">
            <nav id="top_menu" class="navbar navbar-expand-lg navbar-light">
                <div class="container">
                    <!-- Brand Logo -->
                    <a class="navbar-brand" href="/">
                        <span class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
                            <t t-esc="'Tailwind'"/>
                        </span>
                    </a>
                    
                    <!-- Mobile Toggle -->
                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    
                    <!-- Navigation Links -->
                    <div class="collapse navbar-collapse" id="navbarContent">
                        <ul class="navbar-nav ml-auto align-items-center">
                            <li class="nav-item">
                                <a class="nav-link" href="/#features">
                                    <t t-esc="'Features'"/>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/#about">
                                    <t t-esc="'About'"/>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/#services">
                                    <t t-esc="'Services'"/>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/#pricing">
                                    <t t-esc="'Pricing'"/>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/#blog">
                                    <t t-esc="'Blog'"/>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/#contact">
                                    <t t-esc="'Contact'"/>
                                </a>
                            </li>
                            <li class="nav-item ml-3">
                                <a class="header-cta-btn" href="/contactus">
                                    <t t-esc="'Get Started'"/>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </xpath>
    </template>
    
</odoo>
